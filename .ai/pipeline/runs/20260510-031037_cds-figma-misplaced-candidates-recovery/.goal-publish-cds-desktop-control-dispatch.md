# Desktop Control Dispatch

You are executing one bounded desktop-control stage for a `-play` goal run.

Repo: `/Users/zenkim_office/Project/CDS`
Run directory: `/Users/zenkim_office/Project/CDS/.ai/pipeline/runs/20260510-031037_cds-figma-misplaced-candidates-recovery`
Stage: `publish-cds`
Stage prompt file: `/Users/zenkim_office/Project/CDS/.ai/pipeline/runs/20260510-031037_cds-figma-misplaced-candidates-recovery/desktop-publish-cds.md`
Required result JSON file: `/Users/zenkim_office/Project/CDS/.ai/pipeline/runs/20260510-031037_cds-figma-misplaced-candidates-recovery/.goal-publish-cds-desktop-control-result.json`

## Required Result Contract

When this desktop stage is complete, write exactly one JSON object to the result file path above:

```json
{"status":"done","summary":"what changed or what was completed","updated_at":"2026-05-10T04:03:23+0900","changed_files":[]}
```

Use `status:"needs_user_decision"` if the action needs the user, local OS permissions, account login, 2FA, payment, irreversible publish confirmation, or external account permission changes.
Use `status:"failed"` if the stage cannot be completed safely.

## Safety Boundaries

- Do not approve 2FA, payment, irreversible publish confirmation, or external account permission changes unless the user has explicitly approved that final action in the stage prompt.
- Keep the action limited to this stage. Do not start unrelated implementation work.
- If your runtime does not expose desktop-control tools, write `needs_user_decision` to the result file instead of pretending the action completed.

## Stage Prompt

# Desktop Stage: Publish CDS Feed Creation Components

The user explicitly approved this bounded publish/update stage.

## Goal

Publish the newly created CDS library components in Figma so they can be imported into the usage file.

## Target

- Figma app
- File tab: `CDS`
- File key: `H36eNEd6o7ZTv4R7VcyLf2`
- New group on canvas: `Feed Creation`
- New source components:
  - 27 generic recovery components under `Feed Creation`
  - 1 state-specific component `Feed Compose Option Row Tag People`

## Action

1. In Figma Desktop, open the `CDS` tab if it is not already active.
2. Open the Libraries / resources publish flow. The UI currently shows a blue dot on the Libraries icon, indicating unpublished changes.
3. Publish the new/changed library components from the current file.
4. If Figma asks for a publish description, use:
   `Recover Feed Creation candidate components into CDS source file`
5. Do not change unrelated files or components.

## Required Result

Write the result JSON to the path provided by the desktop actuator. Use:

```json
{"status":"done","summary":"Published CDS Feed Creation recovery components","updated_at":"<timestamp>","changed_files":[]}
```

If OS permissions, login, account prompts, or publish permissions block the action, write `needs_user_decision` with a short reason.
