# CDS → Pencil 이관 재플래닝 (사용자 gap discovery 문제)

- **날짜**: 2026-04-21
- **모드**: Team Mode — Problem Solving (Simulation)
- **참석**: AI Ops Expert, Design Director, Engineering Lead
- **주제**: 자동 루프가 gap을 못 잡아서 사용자가 계속 발견해 리포트하는 현상 → 원인 분석 + 재플래닝

---

## Phase 1: 전문가별 분석

### AI Ops Expert
현재 루프의 3계층 실패:
1. **Discovery gap** — 큐가 "이미 만든 31개"로 한정. Figma 원본 200+ 중 존재 모르는 컴포넌트 다수
2. **Depth gap** — top-level 속성만 비교, 내부 anatomy 미대조
3. **Verification gap** — 시각 검증 실패(`get_screenshot` blank)해도 통과로 판정

기준: SSOT 커버리지 < 30% (현재), 목표 95%+. 측정 자체가 없었음.

### Design Director
**Component Completeness 10-Point Rubric** 미적용 상태:
| 항목 | 현황 |
|------|------|
| 존재 여부 | 51/200+ (25%) |
| 치수 정확도 | 70% |
| Fill/Stroke/Radius | 80% |
| 내부 anatomy | 미검증 |
| 텍스트 스펙 | 부분 |
| 아이콘 매핑 | 하드코딩 |
| 인스턴스 활용 | 일부 |
| 토큰 바인딩 | 51→73 진행 |
| variant 커버리지 | flatten 수용 |
| 시각 픽셀 정합 | 표본만 |

체계적 루브릭 없이 reactive하게만 fix한 결과. Storybook-style visual regression 부재.

### Engineering Lead
MCP 한계 (reusable screenshot blank, readDepth 제한, 20KB return 제약)는 툴 문제 아님. **플랜 자체가 incomplete:**

```
현재: for each (my 31) { diff, fix }
필요: enumerate → classify → 3-layer diff → coverage gate
```

---

## Phase 2: 종합 토론

### 합의점
1. Discovery-first 접근 필요 (Figma 전체 enumeration 선행)
2. 3-layer diff (top-level + inner anatomy + rendered PNG)
3. Priority는 Challify 프로덕트 파일 usage 기준 (fileKey `t0SK7XaNqw8qIY3PpZw4s7`)
4. Coverage gate로 완결 판정 필요 (현재 판정 기준 부재)

### 충돌점
- AI Ops: Self-improvement loop (사용자 피드백 규칙화) 강조
- Design Director: 10-Point Rubric 명시화 우선
- Engineering Lead: 4-Phase 파이프라인 우선

세 관점 보완적 → 통합 가능

### 추천 — NEW PLAN v2

```
Phase 0: Discovery (15분)
  discovery.json = Figma CDS 전체 top-level 컴포넌트
  usage.json = Challify product 각 컴포넌트 인스턴스 카운트
  priority.json = P0(사용 상위 20%)/P1(중간 50%)/P2(하위 30%)

Phase 1: Coverage Audit (30분)
  현재 51종 Figma 매칭률 리포트
  P0 누락 Top N 식별
  COVERAGE-REPORT.md 산출

Phase 2: 3-Layer Diff (30분)
  Layer 1: top-level spec
  Layer 2: inner anatomy (child tree 재귀 비교)
  Layer 3: rendered PNG (export_nodes + image diff)

Phase 3: Coverage Gate
  P0 100% + P1 80% + P2 best-effort

Phase 4: Drift Monitoring
  discovery.json 스냅샷 + 변경 감지
```

### 결정 기준
- Discovery 안 하면 방향 잃음 → Phase 0 생략 불가
- 3-layer diff 없으면 "padding 너무 얇아" 류 재발 → Phase 2 생략 불가
- Challify 프로덕트 usage 가중치 없으면 사용 빈도 낮은 컴포넌트에 시간 낭비 → Phase 0의 usage.json 핵심

---

## Next Actions

1. **지금 세션에서 Phase 0 착수** — Figma CDS 전체 enumeration + Challify 프로덕트 파일 usage count
2. **Coverage 리포트** — 51 reusables의 매칭률 + P0 누락 목록 (`COVERAGE-REPORT.md`)
3. **3-Layer Diff 프로토콜 문서화** — `.ralph/PROMPT.md` 교체

---

*Generated by Lenny's Product Team — Team Mode*
