# 초대 수락/거절 바텀시트 컴포넌트화 결정

- **날짜**: 2026-03-12
- **모드**: Team Mode — Decision Meeting
- **참석**: Design Director, Engineering Lead
- **주제**: 1곳에서만 사용되는 바텀시트(초대 수락/거절)를 TDS 컴포넌트로 만들어야 하는가?

---

## Figma 구조 (node-id: 18003-1974)

- Section/ActionArea: bg-black opacity 90% (dim overlay)
  - Card/InviteBanner: Avatar XL + 초대 메시지
  - Button (Type=Inverted, Size=Large): "초대 수락하기"
  - Button (Type=Ghost, Size=Default): "거절하기"
  - iOS/HomeIndicator

## Phase 1: 전문가별 분석

### Design Director

**디자인 시스템 컴포넌트 추가 기준** 적용:
- 3회 이상 재사용 → 컴포넌트화
- 1-2회 사용 → 인라인 처리

현재 1곳 사용 → 기준 미달. 구성 요소(Avatar, Button)는 이미 TDS에 존재.

Bottom Sheet Container(dim + sheet + indicator)는 범용 래퍼로 별도 컴포넌트화 가치 있음.

### Engineering Lead

**Start Simple** 원칙: 1곳에서만 쓰이는 UI를 컴포넌트로 추출하면 불필요한 추상화 + 유지보수 부담.

Bottom Sheet Container는 알림시간/각오/초대 등 여러 곳에서 재사용 가능 → 컴포넌트화 타당.

## Phase 2: 종합 토론

### 합의점
- 초대 수락/거절 바텀시트 전체를 독립 TDS 컴포넌트로 만들 필요 없음
- 구성 요소는 이미 TDS에 존재하므로 Atom 조합으로 충분
- Bottom Sheet Container(dim + sheet + indicator)는 별도 컴포넌트화 가치 있음

### 충돌점
- 없음 (양쪽 동일 결론)

### 트레이드오프

| 옵션 | 장점 | 단점 | 지지 |
|------|------|------|------|
| A. 통째로 컴포넌트화 | Figma 드래그앤드롭 편의 | 1회용, 라이브러리 오염 | 없음 |
| B. 인라인 (기존 Atom 조합) | 유연함, 관리 부담 없음 | 매번 직접 배치 | 양쪽 |
| C. Bottom Sheet Container만 컴포넌트 | 공통 패턴 재사용 | Container 설계 비용 | 양쪽 |

### 추천

**옵션 B + C 조합.** 초대 화면은 기존 Atom 인라인 조합으로 처리. Phase 3의 Bottom Sheet Container를 범용 래퍼로 만들어서 다른 바텀시트(알림시간, 각오)와 함께 사용.

### Next Actions
1. Phase 3 Bottom Sheet Container 설계 시 초대 화면도 래퍼 안에 들어가는지 검증
2. 초대 바텀시트는 프로덕트 디자인에서 기존 TDS Atom 인스턴스로 조합
3. Bottom Sheet Container 공통 요소 정의 (dim opacity, border-radius, padding, handle 유무)

---
*Generated by Lenny's Product Team — Team Mode*
